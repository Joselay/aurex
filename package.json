{
  "name": "aurex-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test cloudflare-worker/test/*.test.js",
    "check:cloudflare": "node --check cloudflare-worker/src/index.js",
    "deploy:cloudflare": "npx wrangler deploy --config cloudflare-worker/wrangler.toml",
    "tail:cloudflare": "npx wrangler tail aurex"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
